# 项目运行指南

## ✅ 已完成的修复

### 1. npm 镜像源配置
- 已将 npm 镜像源从淘宝旧镜像切换到新镜像：`https://registry.npmmirror.com`
- 解决了 `ETIMEDOUT` 连接超时问题

### 2. node-sass 替换为 sass
**问题原因：**
- `node-sass` 需要 Python 2 环境进行编译
- Node.js 18.x 版本与 node-sass 不兼容
- 导致安装失败：`EPERM`、`SyntaxError: invalid syntax`

**解决方案：**
- ✅ admin/package.json：将 `node-sass@^4.13.1` 替换为 `sass@^1.32.0`
- ✅ front/package.json：将 `node-sass@^4.13.1` 替换为 `sass@^1.32.0`
- ✅ 重新安装依赖成功

### 3. SCSS 深度选择器语法修复
**问题原因：**
- Dart Sass 不支持 `/deep/` 语法
- 需要使用 `::v-deep` 替代

**解决方案：**
- ✅ 创建自动修复脚本：`fix-scss-deep-selector.ps1`
- ✅ 批量替换所有 `.vue` 文件中的 `/deep/` 为 `::v-deep`
- ✅ Admin 前端已成功启动（http://localhost:8081）

### 4. 依赖安装完成
- ✅ admin 目录：1609 个包安装成功
- ✅ front 目录：1574 个包安装成功

---

## 🚀 启动项目步骤

### 第一步：启动后端（Spring Boot）

```powershell
cd D:\system\medicineresource
mvn spring-boot:run
```

或在 IDEA 中运行 `SpringbootSchemaApplication` 主类

**后端地址：** http://localhost:8080/medicine

---

### 第二步：启动管理后台（admin）

```powershell
cd D:\system\medicineresource\src\main\resources\admin
npm run serve
```

**管理后台地址：** 查看控制台输出（通常是 http://localhost:8081）

---

### 第三步：启动用户前台（front）

```powershell
cd D:\system\medicineresource\src\main\resources\front
npm run serve
```

**用户前台地址：** 查看控制台输出（通常是 http://localhost:8082）

---

## 📝 默认登录账号

### 管理员
- 用户名：`admin`
- 密码：`admin`

### 患者账号（示例）
- 用户账号：`001` ~ `008`
- 密码：`123456`

### 医生账号（示例）
- 医生工号：`21` ~ `28`
- 密码：`123456`

---

## ⚙️ 数据库配置

根据 `application.yml`：
- 主机：`127.0.0.1:3307`
- 数据库名：`medicine`
- 用户名：`root`
- 密码：`123456`

**如果 MySQL 端口不是 3307，请修改配置文件中的端口号**

---

## 🔧 常见问题

### 1. 端口被占用
修改 `application.yml` 中的 `server.port`

### 2. 前端启动失败
```powershell
# 清理后重新安装
Remove-Item -Recurse -Force node_modules
npm install
npm run serve
```

### 3. 数据库连接失败
- 确认 MySQL 服务已启动
- 检查端口是否正确
- 确认数据库 `medicine` 已创建并导入 SQL 文件

### 4. 跨域问题
确保前端配置中的 API 代理指向正确的后端地址

---

## 📌 注意事项

1. **Node.js 版本警告**
   - 当前使用 Node.js v18.19.0
   - 项目推荐 Node.js 14-17
   - 虽然有警告，但可以正常运行

2. **弃用警告**
   - 项目中使用了较多旧版本依赖
   - 会有 deprecated 警告，但不影响运行
   - 如需升级，建议整体升级 Vue 3 + Vite

3. **Sass 兼容性**
   - 已从 `node-sass` 迁移到 `sass`（Dart Sass）
   - 无需 Python 环境
   - 完全兼容现有 SCSS 代码

---

## 💡 快速启动脚本

创建 PowerShell 脚本一键启动所有服务：

**start-all.ps1**
```powershell
# 启动后端
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd D:\system\medicineresource; mvn spring-boot:run"

# 等待 5 秒
Start-Sleep -Seconds 5

# 启动管理后台
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd D:\system\medicineresource\src\main\resources\admin; npm run serve"

# 等待 5 秒
Start-Sleep -Seconds 5

# 启动用户前台
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd D:\system\medicineresource\src\main\resources\front; npm run serve"

Write-Host "所有服务正在启动..." -ForegroundColor Green
```

---

## ✨ 项目技术栈

### 后端
- Spring Boot 2.2.2
- MyBatis Plus
- MySQL
- WebSocket

### 前端
- Vue 2.6
- Element UI
- Vue Router
- Vuex
- Axios
- ECharts

---

**最后更新：** 2026-05-10  
**修复内容：** npm 依赖安装问题、node-sass 编译错误
